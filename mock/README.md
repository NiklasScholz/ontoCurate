Install backend:

```sh
cd backend
python -m venv venv
source ./venv/bin/activate
pip install -r requirements.txt
```

Run backend:

```sh
fastapi dev
```

Install frontend:

```sh
cd frontend
npm install
```

Run frontend:

```sh
npm run dev
```
